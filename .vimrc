" Set utf8 as standard encoding and en_US as the standard language
set encoding=utf8
" Enable mouse for all modes
set mouse=a
" Command-line completion matches are show just above the command line
set wildmenu        
" Display the line number in front of each line
set number
" Highlight the screen line of the cursor
set cursorline
" Highlight the screen column of the cursor
set cursorcolumn
" Display the line & column number of the cursor position
set ruler
" Minimal number of lines to keep above & below the cursor
set scrolloff=4
" Enable syntax highlighting
syntax on
" Set colorscheme to delek
colorscheme industry
" Enable smart indenting when starting a new line
set smartindent
" Number of spaces to use for each step of (auto)indent
set shiftwidth=4
" Number of spaces that a <tab> in the file counts for
set tabstop=4
" Highlight matching [ { ( ) } ]
set showmatch
" Search as characters are entered
set incsearch
" Highlight all search matches
set hlsearch
" Ignore case in search patterns
set ignorecase
" Override 'ignorecase' option when upper case characters are used
set smartcase
